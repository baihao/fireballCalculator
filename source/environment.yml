# 可选：在网络正常且需纯 conda 解环境时使用
#   conda env create -f environment.yml
# 默认推荐：使用 setup.sh + requirements.txt（仅用 conda 装 Python，其余 pip），避免 conda-forge 连接问题
#
# 需满足 document/fireball_gp_mogp_module_design.md：Python 3.10+、PyTorch、GPyTorch 等
name: fireball_calculator
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
